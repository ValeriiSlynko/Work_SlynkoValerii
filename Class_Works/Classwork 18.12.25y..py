# ЗАВДАННЯ 1: «Комуналка за електроенергію»
# Умова
# Користувач вводить споживання електроенергії за місяць у кВт год.
# Потрібно порахувати суму до сплати за тарифними діапазонами.
# Вимоги (що зробити)
# Зчитати kwh (ціле число).
# Порахувати оплату по частинах:
# перші 100 кВт·год → 1.20 грн/кВт год
# наступні 200 (тобто 101–300) → 1.50 грн/кВт·год
# усе, що понад 300 → 1.80 грн/кВт год
# Вивести:
# загальну суму
# окремо: скільки кВт·год у кожному діапазоні
# Приклад вводу/виводу
# Ввід (що вводять):
# Введіть споживання (кВт·год): 350
# Вивід (що вивести):
# До 100: 100 кВт·год
# 101-300: 200 кВт·год
# Понад 300: 50 кВт·год
# Сума: 525.0 грн
# Додаткові коментарі (рекомендації)
# Якщо kwh < 0 — вивести Помилка вводу.
# Розв’язання тільки через if / elif / else і арифметику (без циклів, без методів).

kwh = float(input("Споживання електроенергії за місяць кВт/год: "))
if kwh < 0:
    print("Помилка вводу")
elif kwh <= 100:
    d1 = kwh
    d2 = 0
    d3 = 0
    total = d1 * 1.20
    print("До 100:", d1, "кВт·год")
    print("101-300:", d2, "кВт·год")
    print("Понад 300:", d3, "кВт·год")
    print("Сума:", total, "грн")
elif kwh <= 300:
    d1 = 100
    d2 = kwh - 100
    d3 = 0
    total = d1 * 1.20 + d2 * 1.50
    print("До 100:", d1, "кВт·год")
    print("101-300:", d2, "кВт·год")
    print("Понад 300:", d3, "кВт·год")
    print("Сума:", total, "грн")
else:
    d1 = 100
    d2 = 200
    d3 = kwh - 300
    total = d1 * 1.20 + d2 * 1.50 + d3 * 1.80
    print("До 100:", d1, "кВт·год")
    print("101-300:", d2, "кВт·год")
    print("Понад 300:", d3, "кВт·год")
    print("Сума:", total, "грн")

# ЗАВДАННЯ 2: «Кінотеатр: ціна квитка»
# Умова
# Ціна квитка залежить від віку та дня тижня.
# Вимоги (що зробити)
# Зчитати:
# age (int)
# day (int, 1–7), де 6–7 — вихідні
# Базова ціна:
# будні (1–5) - 160 грн
# вихідні (6–7) - 200 грн
# Знижка за віком:
# age < 6 → квиток безкоштовно (0 грн)
# 6 <= age <= 12 - -50%
# age >= 60 - -30%
# інакше - без знижки
# Вивести фінальну ціну.
# Приклад вводу/виводу
# Ввід (що вводять):
# Введіть вік: 10
# Введіть день тижня (1-7): 7
# Вивід (що вивести):
# Ціна квитка: 100 грн
# Додаткові коментарі (рекомендації)
# Якщо day не 1–7 або age < 0 - вивести Помилка вводу.
# Не використовувати try/except і методи рядків.
age = int(input("Введіть вік: "))
day = int(input("Введіть день тижня (1-7): "))

if age < 0 or day < 1 or day > 7:
    print("Помилка вводу")
else:
    if day <= 5:
        price = 160
    else:
        price = 200
    if age < 6:
        price = 0
    elif age <= 12:
        price = price * 0.5
    elif age >= 60:
        price = price * 0.7
    print("Ціна квитка:", price, "грн")

# ЗАВДАННЯ 3: «Пора року за номером місяця» (match)
# Умова
# Користувач вводить номер місяця month (1–12). Вивести пору року і назву кварталу.
# Вимоги (що зробити)
# Зчитати month (int).
# Використати match month:
# 12, 1, 2 → Зима
# 3, 4, 5 → Весна
# 6, 7, 8 → Літо
# 9, 10, 11 → Осінь
# інакше → Невірний місяць
# Додатково визначити квартал:
# 1–3 → Q1, 4–6 → Q2, 7–9 → Q3, 10–12 → Q4
# Приклад вводу/виводу
# Ввід (що вводять):
# Введіть номер місяця (1-12): 4
# Вивід (що вивести):
# Пора року: Весна
# Квартал: Q2
# Додаткові коментарі (рекомендації)
# Квартал можна зробити через if/elif/else, а пору року - через match.
# Без списків/словників.
month = int(input("Введіть номер місяця (1-12): "))

match month:
    case 12 | 1 | 2:
        season = "Зима"
    case 3 | 4 | 5:
        season = "Весна"
    case 6 | 7 | 8:
        season = "Літо"
    case 9 | 10 | 11:
        season = "Осінь"
    case _:
        season = None

if season is None:
    print("Невірний місяць")
else:
    print("Пора року:", season)

    if month <= 3:
        quarter = "Q1"
    elif month <= 6:
        quarter = "Q2"
    elif month <= 9:
        quarter = "Q3"
    else:
        quarter = "Q4"

    print("Квартал:", quarter)
